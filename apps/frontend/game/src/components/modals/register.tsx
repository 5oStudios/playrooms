import { BaseModal } from './base-modal';

export const Register = () => {
  return (
    <BaseModal
      isOpen={true}
      onClose={() => {
        //
      }}
    >
      <div>Register</div>
    </BaseModal>
  );
};
