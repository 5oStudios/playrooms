import { BaseModal } from '@components';

export const Register = () => {
  return (
    <BaseModal
      isOpen={true}
      onClose={() => {
        //
      }}
    >
      <div>Register</div>
    </BaseModal>
  );
};
